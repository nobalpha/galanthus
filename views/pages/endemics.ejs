<!DOCTYPE html>
<html lang="en">
    <head>
        <%- include('../partials/head'); %>
        <link rel="stylesheet" href="/css/identification.css" />
        <link rel="stylesheet" href="/css/societies.css" />

        <title>Endemic Flora in Turkey</title>
    </head>
    <body>
        <%- include('../partials/logo'); %>

        <div class="societies">
            <% if (locals.data) { for (let i = 0; i < data.length; i++) { %>
            <div class="card mb-3" style="max-width: 540px">
                <div class="row g-0">
                    <div class="col-md-4">
                        <img
                            src="<%=data[i].src%>"
                            alt="Image of <%=data[i].name%>"
                            style="
                                width: 80%;
                                border-radius: 10px;
                                margin-top: 5%;
                                margin-bottom: 5%;
                            "
                        />
                    </div>
                    <div class="col-md-8">
                        <div class="card-body">
                            <h5
                                class="card-title"
                                style="margin-top: 10%; vertical-align: middle"
                            >
                                <b><%=data[i].title%></b>
                            </h5>
                        </div>
                    </div>
                </div>
            </div>
            <%} }%>
        </div>
        <form method="get" action="/endemics">
            <%for(let i = 0; i < locals.totalPages; i++) {%>
            <input
                class="btn-success btn-sm"
                type="submit"
                name="page"
                value="<%=i + 1%>"
            />
            <%}%>
        </form>
        <footer><%- include('../partials/footer'); %></footer>
    </body>
</html>
